@AGENTS.md

# Claude Code — Discover Nashik
Read `CONTEXT.md` first. You own: `lib/` (except `lib/firebase.ts`, `lib/i18n/`), `app/api/`, `public/sw.js`, `app/manifest.ts`, `data/places.json`, `scripts/`.
Never edit `/types`. Work on `feat/core-*` branches. Run `npm run build` and `npm run data` before saying you're done.
