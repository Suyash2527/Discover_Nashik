# Discover Nashik
Kumbh Mela digital companion — MET IoE × GDG Nashik hackathon.

```bash
npm install
cp .env.example .env.local   # fill keys
npm run data                 # CSV → data/places.json + validation
npm run dev
```
Agents: read `CONTEXT.md`. Full plan: `EXECUTION_PLAN.md`.
